diff --git a/cmake_modules/orc.diff b/cmake_modules/orc.diff
index e4ca4e2..c7b5510 100644
--- a/cmake_modules/orc.diff
+++ b/cmake_modules/orc.diff
@@ -435,3 +435,63 @@ index 9b2c829c7..434841224 100644
  set(ORC_FORMAT_VERSION "1.0.0")
  set(LZ4_VERSION "1.10.0")
  set(SNAPPY_VERSION "1.2.1")
+diff --git a/c++/src/Timezone.cc b/c++/src/Timezone.cc
+--- a/c++/src/Timezone.cc
++++ b/c++/src/Timezone.cc
+@@ -26,6 +26,8 @@
+ #include <time.h>
+ #include <atomic>
+ #include <map>
++#include <exception>
++#include <mutex>
+ #include <sstream>
+ 
+ namespace orc {
+@@ -691,18 +693,43 @@
+     return buffer;
+   }
+ 
++  // Paimon: LazyTimezone no longer uses std::call_once (mixed libstdc++ can mishandle
++  // call_once+exceptions). We use a mutex; on load failure we try GMT/UTC fallbacks.
+   class LazyTimezone : public Timezone {
+    private:
+     std::string filename_;
+     mutable std::unique_ptr<TimezoneImpl> impl_;
+-    mutable std::once_flag initialized_;
++    mutable std::mutex init_mutex_;
+ 
+     TimezoneImpl* getImpl() const {
+-      std::call_once(initialized_, [&]() {
++      std::lock_guard<std::mutex> lock(init_mutex_);
++      if (impl_) {
++        return impl_.get();
++      }
++      std::exception_ptr primary_error;
++      try {
+         auto buffer = loadTZDB(filename_);
+         impl_ = std::make_unique<TimezoneImpl>(filename_, std::move(buffer));
+-      });
+-      return impl_.get();
++        return impl_.get();
++      } catch (...) {
++        primary_error = std::current_exception();
++      }
++      const std::string tzdir = getTimezoneDirectory();
++      static const char* kFallbackZones[] = {"GMT", "UTC", "Etc/UTC"};
++      for (const char* zone : kFallbackZones) {
++        const std::string path = tzdir + "/" + zone;
++        if (!fileExists(path.c_str())) {
++          continue;
++        }
++        try {
++          auto buffer = loadTZDB(path);
++          impl_ = std::make_unique<TimezoneImpl>(path, std::move(buffer));
++          return impl_.get();
++        } catch (const std::exception&) {
++          // try next
++        }
++      }
++      std::rethrow_exception(primary_error);
+     }
+ 
+    public:
