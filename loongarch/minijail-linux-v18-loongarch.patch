--- a/arch.h
+++ b/arch.h
@@ -87,6 +87,15 @@
 #    define AUDIT_ARCH_SPARC
 #    define MINIJAIL_ARCH_NAME "sparc"
 #  endif
+#elif defined(__loongarch64)
+/*
+ * AUDIT_ARCH_LOONGARCH64 may not be defined in older kernel headers.
+ */
+#  ifndef AUDIT_ARCH_LOONGARCH64
+#    define AUDIT_ARCH_LOONGARCH64 0xC0000102
+#  endif
+#  define MINIJAIL_ARCH_NR AUDIT_ARCH_LOONGARCH64
+#  define MINIJAIL_ARCH_NAME "loongarch64"
 #else
 #  error "AUDIT_ARCH value unavailable"
 #endif
--- a/gen_constants-inl.h
+++ b/gen_constants-inl.h
@@ -34,6 +34,9 @@
  */
 #undef BLKTRACESETUP
 #undef FS_IOC_FIEMAP
+/* On LoongArch, FPU_ALIGN is defined as __attribute__((aligned(8))) in
+ * signal.h, not a numeric constant, so remove it to avoid build errors. */
+#undef FPU_ALIGN

 /* The old glibc bundled with the Android host toolchain is missing some ioctl
  * definitions used by minijail policy in crosvm and other projects. Locally
--- a/util.c
+++ b/util.c
@@ -66,6 +66,8 @@
 const char *const log_syscalls[] = {"socket", "connect", "send"};
 #elif defined(__riscv)
 const char *const log_syscalls[] = {"socket", "connect", "sendto"};
+#elif defined(__loongarch64)
+const char *const log_syscalls[] = {};
 #else
 #error "Unsupported platform"
 #endif
